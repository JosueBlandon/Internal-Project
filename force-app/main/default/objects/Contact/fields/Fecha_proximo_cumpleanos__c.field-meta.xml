<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Fecha_proximo_cumpleanos__c</fullName>
    <externalId>false</externalId>
    <formula>IF(TODAY()&gt;DATE(YEAR(TODAY()),MONTH(Birthdate),DAY(Birthdate)),DATE(YEAR(TODAY())+1,MONTH(Birthdate),DAY(Birthdate)),DATE(YEAR(TODAY()),MONTH(Birthdate),DAY(Birthdate)))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Fecha próximo cumpleaños</label>
    <required>false</required>
    <type>Date</type>
</CustomField>
